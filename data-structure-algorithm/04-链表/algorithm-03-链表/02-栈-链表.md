

## 栈的实现
```text

将栈保存为一条链表：
顶部指向表头，实例变量first 指向栈顶

push 就将元素加入表头
pop ： 在表头删除
size(): 用实例变量 N 保存元素的个数，在压入将N+1;弹出N-1
isEmpty(): 只需检查first 是否 null 或 N 是否 0

链表的使用达到最优设计目标：
1.可以处理任意类型数据
2.所需空间总是和集合成正比
3.操作所需的时间和集合大小无关
```